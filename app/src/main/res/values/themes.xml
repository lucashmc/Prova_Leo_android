<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Prova_leo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Cores principais do tema verde -->
        <item name="colorPrimary">@color/primary_green</item>
        <item name="colorPrimaryVariant">@color/primary_green_dark</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Cor secundária -->
        <item name="colorSecondary">@color/secondary_green</item>
        <item name="colorSecondaryVariant">@color/primary_green_light</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Cor de fundo -->
        <item name="android:windowBackground">@color/background_light_green</item>

        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/primary_green_dark</item>
    </style>

    <style name="Theme.Prova_leo" parent="Base.Theme.Prova_leo" />
</resources>